{
  
  "name": "blog-agi-automation",
  "version": "1.0.0",
  "description": "Projeto de testes automatizados do campo busca",
  "scripts": {
    "cypress:run": "cypress run",
    "report:generate": "npx mochawesome-merge cypress/reports/*.json > cypress/reports/report.json && npx marge cypress/reports/report.json -f report -o cypress/reports",
    "test:report": "npm run cypress:run && npm run report:generate"
  },

  "devDependencies": {
    "cypress": "^15.13.1",
    "mochawesome": "^7.1.4",
    "mochawesome-merge": "^5.1.1",
    "mochawesome-report-generator": "^6.3.2"
  }
  
}


